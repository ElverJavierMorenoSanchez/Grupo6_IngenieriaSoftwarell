package Interfaces;

public interface ValidationCrud {
    public boolean validationNumberPhone(String number);
    
    public boolean validationPassword (String password);
    
    public boolean validationIdentityCard(String identityCard);
}
